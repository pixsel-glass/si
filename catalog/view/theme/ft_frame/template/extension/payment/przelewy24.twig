{#<form action="{{ go_url }}" method="post">
  <input type="hidden" name="operation_xml" value="{{ xml }}">
  <input type="hidden" name="signature" value="{{ signature }}">
  <div class="buttons">
    <div class="pull-right">
      <input type="submit" value="{{ button_confirm }}" class="btn btn-primary" />
    </div>
  </div>
</form>
<a href="{{go_url}}" class="btn btn-primary" target="_blank" id="go_url">Płacić</a>
{{go_url}}#}

<div class="buttons">
  <div class="pull-right">
    <input type="button" value="{{ button_confirm }}" id="button-confirm" class="btn btn-primary" />
  </div>
</div>
<script type="text/javascript"><!--
  $('#button-confirm').bind('click', function() {
    location.href = "{{ go_url }}";
  });
//--></script>